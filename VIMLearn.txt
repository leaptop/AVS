VIM LEARNING
gg - go to the first line
dG - delete all lines before the current one
Ctrl [, Esc, Ctrl q - escape from the insert mode
If you hit Ctrl s in insert mode - getting out is made by Ctrl q
:w - write the changes to the file
"%b" allows us to use escape sequences
%d refuses to print anything, but integers
if a number starts with 0 - it means, that it's octal, apparently
date --date="today" +%F - here +%F formats the date to Russian style
%Y-%m%-%d - makes the same

By default ubuntu doesn't have root user(su or super user) But you should type sudo before commands to tell the computer, that you are a "super user". Or yu can imitate the super user rejime by typing sudo -i. If you want to create a password for root, type: sudo password root. You shouldn't forget the password. To exit the su regime type: exit.

To copy-paste position a cursor, press v, use arrows to select the text, press d(delete) or y(yank) to copy, choose a new position, press p to paste after the cursor or P to paste before.

u - undo, Ctrl r - redo

To allow a bash script or other file to work type: chmod +x ./l1.sh(or other filename)

You can call a comand in a bash file directly, like:
df -H | grep "/dev/sda5" | awk '{print $2}'
or you can call it through printf:
printf  "$(df -H | grep "/dev/sda5" | awk '{print $2}')"

Ctrl o - returns a carete to the previous position

man ifconfig - watch manual on the ifconfig command

/word - search for "word" in terminal
n- next found word, N - previous

clock ticks is the same as frequency of CPU(2.4*10^9 Herz in my case). 2.4 billion ticks per second/

to print long numbers in c: %li (long int)

An interval is a contiguous part of the time continuum between two calendar times, for example the hour between 9:00 and 10:00 on July 4, 1980.

An elapsed time is the length of an interval, for example, 35 minutes. People sometimes sloppily use the word “interval” to refer to the elapsed time of some interval.

#if 1 //1 - do, 0 - do not
//some actions
#endif - an interesting way of making if statement
